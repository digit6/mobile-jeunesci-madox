mutation($password:String,$id:ID!) {
 updateJeune(input:{
  where:{id:$id}
  data:{password:$password}
})
  {jeune{
    id
    nom
      email
      contact
      password
      profession
      auth
      createdAt
      updatedAt
    
    
  }}
}

